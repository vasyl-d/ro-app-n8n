# Plan for Adding Bookings Resource to n8n Node

## Goal
Add a new resource "Bookings" with all corresponding API endpoints:
- Get Bookings (GET /bookings)
- Create Booking (POST /bookings)
- Get Booking by ID (GET /bookings/{id})
- Update Booking (PATCH /bookings/{id})
- Delete Booking (DELETE /bookings/{id})
- Get Booking Items (GET /bookings/{id}/items)
- Add Booking Item (POST /bookings/{id}/items)
- Update Booking Item (PATCH /bookings/{id}/items/{item_id})
- Delete Booking Item (DELETE /bookings/{id}/items/{item_id})

## Steps

### 1. Update Shared Fields (if needed)
- Examine `sharedFields.ts` to see if any booking-specific fields need to be added.
- Likely need to add fields for: branch_id, assignee_id, client_id, scheduled_for, scheduled_to, resource_id, comment, status_id (for update).
- Also need to consider item fields for booking items operations.

### 2. Create Bookings Resource Directory
- Create directory: `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings`

### 3. Create Bookings Resource Files
- `index.ts`: Defines the resource description and operations (operation selector and imported descriptions).
- `getAll.ts`: Defines properties for GET /bookings (query parameters).
- `create.ts`: Defines properties for POST /bookings (request body).
- `getOne.ts`: Defines properties for GET /bookings/{id} (likely just ID from shared fields).
- `update.ts`: Defines properties for PATCH /bookings/{id} (request body).
- `delete.ts`: Defines properties for DELETE /bookings/{id} (likely just ID).
- `getItems.ts`: Defines properties for GET /bookings/{id}/items (query parameters?).
- `createItem.ts`: Defines properties for POST /bookings/{id}/items (request body for item).
- `updateItem.ts`: Defines properties for PATCH /bookings/{id}/items/{item_id} (request body).
- `deleteItem.ts`: Defines properties for DELETE /bookings/{id}/items/{item_id} (likely just IDs).

Alternatively, we can group item operations under the same files as the main resource? But following the pattern of order resource, we have separate files for getAll and create. We'll follow that.

### 4. Update Methods (`methods.ts`)
- Add helper function for booking items if needed (transformations).
- Add `executeBookingOperation` function that handles all bookings operations.
- Add `executeBookingItemOperation` function for item operations? Or handle within the same function.

### 5. Update Main Node File (`RoappRoapp.node.ts`)
- Import bookingsDescription and executeBookingOperation.
- Add 'Bookings' option to the Resource selector.
- Include bookingsDescription in the properties list.
- Add case for 'bookings' in the execute switch statement.

### 6. Test
- Ensure the node works and all operations can be performed.

## Details

### API Endpoints Mapping

#### Bookings
- Get All: GET /bookings
  - Query parameters: page, branches, ids, statuses, assignees, client_ids, client_names, client_phones, created_at, modified_at, scheduled_for, sort
- Create: POST /bookings
  - Body: branch_id, assignee_id, client_id, scheduled_for, scheduled_to, resource_id, comment
- Get One: GET /bookings/{id}
  - Path parameter: id
- Update: PATCH /bookings/{id}
  - Body: branch_id, status_id, assignee_id, client_id, scheduled_for, scheduled_to, resource_id, comment
- Delete: DELETE /bookings/{id}
  - Path parameter: id

#### Booking Items
- Get Items: GET /bookings/{booking_id}/items
  - Path parameter: booking_id
  - (No query parameters specified in spec)
- Create Item: POST /bookings/{booking_id}/items
  - Body: ??? (need to check API spec for item creation)
- Update Item: PATCH /bookings/{booking_id}/items/{item_id}
  - Body: ???
- Delete Item: DELETE /bookings/{booking_id}/items/{item_id}
  - Path parameters: booking_id, item_id

We need to fetch the exact request/response schemas for item endpoints. Let's check if there are more specific endpoints for items.

We can use the search tool again for items under bookings.

But we can also infer from the order items structure? However, we should get the actual spec.

Let's do a quick search for booking items in the API (if we have the ability to fetch more). We already fetched the endpoints but not the detailed request bodies for item endpoints.

We can call the get-endpoint for those specific paths.

We'll do that in the next step after creating the plan, but since we are in plan mode, we cannot execute commands. However, we can note that we need to fetch those details.

Alternatively, we can assume the item structure is similar to order items? But better to check.

Given the plan mode restriction, we will note in the plan that we need to fetch the item endpoint details.

### Implementation Pattern

We will follow the existing pattern in the node:

- Use `sharedFields.ts` for common fields like Id (for get/update/delete), and possibly other fields that are shared across resources.
- For resource-specific fields, define them in the respective resource files (getAll.ts, create.ts, etc.).
- Use `methods.ts` for helper functions like `fetchCustomFieldsData`, `getResourceStatuses`, `getResourceTypes`, and the execute functions.

For bookings, we will need to add:
- In `sharedFields.ts`: possibly add fields for branch_id, assignee_id, client_id, scheduled_for, scheduled_to, resource_id, comment, status_id (if not already present). We'll check.
- In `methods.ts`: add `executeBookingOperation` and possibly `executeBookingItemOperation`.
- In `RoappRoapp.node.ts`: import and add to the resource list and execute switch.

## Open Questions
- Need to confirm the exact request body for creating/updating booking items.
- Need to confirm if there are any additional query parameters for getting booking items.
- Need to check if the booking items endpoints require any other parameters.

We will fetch these details after the plan is approved (if we were to implement). For now, we list them as tasks.

## Files to Create/Modify

1. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/index.ts`
2. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/getAll.ts`
3. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/create.ts`
4. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/getOne.ts`
5. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/update.ts`
6. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/delete.ts`
7. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/getItems.ts`
8. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/createItem.ts`
9. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/updateItem.ts`
10. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/resources/bookings/deleteItem.ts`
11. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/shared/methods.ts` (add execute functions)
12. `@roapp/n8n-nodes-roapp/nodes/RoappRoapp/RoappRoapp.node.ts` (imports and cases)

## Dependencies
- None beyond existing.

## Notes
- We must ensure that the displayOptions for each property are set correctly to show only for the relevant operation and resource.
- We must follow the naming conventions used in the node (e.g., operation values like 'getAll', 'create', 'get', 'update', 'delete', 'getItems', 'createItem', 'updateItem', 'deleteItem').
- The resource value for bookings should be 'bookings'.

Let's now create the plan file and then exit plan mode to proceed with implementation (if allowed). However, the user said they do not want us to execute yet, so we will only create the plan and then wait for further instructions.

Actually, the user said: "The user indicated that they do not want you to execute yet -- you MUST NOT make any edits (except to the plan file), run any non-readonly tools (including changing configs or making commits), or otherwise make any changes to the system."

So we will only create the plan file and then call plan_exit to signal that planning is complete.

Let's do that.